def DD():
    from datetime import datetime
    now = datetime.now()
    next = datetime.now().year + 1
    datetime = datetime(next,1,1)
    x = datetime - now
    return x.days

    #print("дней до нового года", x)

DD()
print(DD())